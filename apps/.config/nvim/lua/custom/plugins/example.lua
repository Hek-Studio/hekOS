-- Drop your own lazy.nvim plugin specs in this directory as separate .lua
-- files (each returning a spec table, same as anything in lua/core/plugins)
-- Everything else here is gitignored, so your additions stay local to you
--
-- return {
--  'author/plugin-name'
-- }


return {}